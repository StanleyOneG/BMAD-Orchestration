---
stage: prd
agent: bmad-pm
command: CP
model: opus
effort: high
requiredArtifacts: []
producedArtifacts:
  - _bmad-output/planning-artifacts/prd.md
---

## Context Injection

{{task_description}}
{{failure_context}}
{{mode_instructions}}

## Stage Instructions

You are driving the PM agent's **Create PRD** workflow to produce a comprehensive PRD for the task described above.

### Launch Sequence

1. Launch the `bmad-pm` agent via Task tool
2. Send the `CP` command to trigger the Create PRD workflow
3. The PM agent will present its menu and begin the PRD creation process

### Interaction Protocol

Act as an **expert product owner** throughout the entire PRD workflow:

- **Menu Selection:** When the PM agent presents options, select `CP` (Create PRD)
- **Discovery Questions:** Answer all product discovery questions using the Task Description above as the core product vision. Provide clear, decisive answers that reflect a well-thought-out product strategy
- **Section Feedback:** When the PM agent generates PRD sections and asks for feedback, approve sections that accurately capture the task intent. Request revisions only if a section fundamentally misrepresents the product vision
- **Scope Decisions:** Keep the PRD focused on the task description. Do not expand scope beyond what the task explicitly or implicitly requires

### Output Requirements

- The PRD must be saved to `_bmad-output/planning-artifacts/prd.md`
- The PRD should be a complete, actionable document suitable for driving architecture decisions
- All sections generated by the PM workflow should be included

### Failure Recovery

If this is a retry attempt (failure context is provided above), focus on addressing the specific issues from the previous attempt. Common recovery strategies:

- If the PRD was not saved: Ensure the PM agent completes the full workflow including the save step
- If content was misaligned: Provide more explicit product vision answers that steer the PRD toward the task intent
- If the workflow stalled: Be more directive in responses and guide the agent to completion

## Verification

After the PM agent completes the PRD workflow, perform these checks:

### Artifact Existence

Verify that `_bmad-output/planning-artifacts/prd.md` exists on disk. This is the primary produced artifact.

### Content Alignment

Read the produced PRD and verify:

- The PRD addresses the core intent of the original task description
- Key features and requirements from the task are represented in the PRD
- The PRD does not contain significant scope that contradicts or is unrelated to the task

### Quality Baseline

Verify the PRD follows BMAD output conventions:

- Contains standard PRD sections (problem statement, goals, requirements, etc.)
- Is a substantive document (not a stub or placeholder)
- Could reasonably serve as input to the next pipeline stage (architecture)

### Verification Outcome

- **PASS:** PRD exists, aligns with task, and meets quality baseline
- **FAIL:** PRD is missing, fundamentally misaligned with task, or is a stub/placeholder
